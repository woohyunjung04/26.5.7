{
  "name": "checklist-app",
  "version": "1.0.0",
  "private": true,
  "description": "React + Vite 체크리스트 앱과 Node.js Express 백엔드 예시",
  "scripts": {
    "dev:client": "cd frontend && npm install && npm run dev",
    "dev:server": "cd backend && npm install && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
